import 'package:flutter/material.dart'; // 导入Flutter基础库

class RecordsPage extends StatelessWidget { // 记录页面组件
  const RecordsPage({super.key}); // 构造函数

  @override
  Widget build(BuildContext context) => const Center( // 构建方法
        child: Text('记录页面'), // 临时占位文本
      );
} 